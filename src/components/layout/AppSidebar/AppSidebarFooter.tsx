import { SidebarFooter } from "@/components/ui/sidebar";

const AppSidebarFooter = ({ footer }: { footer: string[] }) => {
  return (
    <SidebarFooter>
      <h1>App Footer</h1>
    </SidebarFooter>
  );
};

export default AppSidebarFooter;

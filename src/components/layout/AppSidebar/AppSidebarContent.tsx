import { SidebarContent } from "@/components/ui/sidebar";

const AppSidebarContent = ({ content }: { content: string[] }) => {
  return <SidebarContent>AppSidebarHeader</SidebarContent>;
};

export default AppSidebarContent;
